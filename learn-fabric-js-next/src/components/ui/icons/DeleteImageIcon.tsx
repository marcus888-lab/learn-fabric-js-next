import React from "react";
import { Icon } from "@iconify/react";
import deleteImageIcon from "@iconify/icons-lucide/trash2";

const DeleteImageIcon: React.FC = () => {
  return <Icon icon={deleteImageIcon} />;
};

export default DeleteImageIcon;
